{
  "name": "bsl-scoreboard",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,web \"nodemon server/index.js\" \"npm --prefix web run dev\"",
    "build": "npm --prefix web run build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "colorthief": "^3.3.1",
    "concurrently": "^9.0.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "fast-average-color": "^9.5.1",
    "multer": "^2.0.2",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
